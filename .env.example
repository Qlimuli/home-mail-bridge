# Beispiel-Umgebung für docker compose
# Kopieren nach .env und anpassen:
#   cp .env.example .env

# Auth-Token für die Administration GUI (dms-gui)
# Erzeugen z. B. mit: openssl rand -hex 24
DMS_GUI_TOKEN=
